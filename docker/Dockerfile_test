# syntax=docker/dockerfile:1.4
FROM python:3.12-alpine

RUN apk add --no-cache make=~4.4 bash=~5.2

WORKDIR /home/microblog

# copy requirement files (scripts use them)
COPY requirements/test.txt requirements/prod.txt requirements/

# copy entrypoint
COPY test.sh /entrypoint-test.sh
RUN chmod +x /entrypoint-test.sh

ENTRYPOINT ["/entrypoint-test.sh"]
